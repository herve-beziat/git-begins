Pour cloner un repository nous avons besoin de faire :
$ git clone https://github.com/YOUR-USERNAME/YOUR-REPOSITORY


$ git add [nom du fichier]
Ajoute un instantanée du fichier, en préparation pour le suivi de version

$ git commit -m "message descriptif"
Enregistre des instatanées de fichier de façon permanente dans l'historique des versions.

$ git push [alias] [branche]
Envoie tout les commits de la branche locale vers github.

$ git pull
Récupère tout l'historique du dépôt nommé et incorpore les modifications

$ git branch [nom-de-branche]
Permet de créer une nouvelle branche

$ git checkout [nom-de-branche]
Bascule sur la branche spécifiée et met à jour le répertoire de travail
